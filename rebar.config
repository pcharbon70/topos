{erl_opts, [debug_info]}.

{deps, []}.

{plugins, []}.

%% Pre-compile hooks for lexer generation
{pre_hooks, [
    {compile, "scripts/build_lexer.sh"}
]}.

{eunit_opts, [verbose]}.

{cover_enabled, true}.
{cover_opts, [verbose]}.
